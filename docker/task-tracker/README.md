Task Tracker - Flask, Redis & Docker Compose
Project Overview

This project is a simple task tracker built to practise running a multi-container application using Docker Compose.

The application uses Flask to serve the web application and Redis to store and increment the number of completed tasks.
Technologies Used

    Python
    Flask
    Redis
    Docker
    Docker Compose
